# 📚 IELTS Platform Frontend (ระบบหน้าบ้าน)

นี่คือส่วน Frontend (ระบบหน้าบ้าน) ของโปรเจกต์แบบทดสอบ IELTS ซึ่งถูกพัฒนาขึ้นด้วย React ผ่าน Vite เพื่อความรวดเร็วในการแสดงผลและการโต้ตอบกับผู้ใช้งาน

## 🚀 สิ่งที่ระบบหน้าบ้านทำได้ (Frontend Features)

- **UI สำหรับเข้าสู่ระบบและสมัครสมาชิก:** หน้าจอสวยงาม ใช้งานง่าย พร้อมการจัดการฟอร์มเพื่อส่งข้อมูลไปยัง Backend
- **การจัดการเส้นทาง (Routing):** มีระบบ Private Route สำหรับป้องกันไม่ให้ผู้ที่ยังไม่ได้ ล็อกอิน เข้าถึงหน้า Dashboard และหน้า Test ได้
- **อินเทอร์เฟซทำแบบทดสอบ (Test Interface):** 
  - สร้าง UI สำหรับการทำข้อสอบ IELTS ที่มีการแบ่งส่วนคำถาม ช่องกรอกคำตอบ
  - มีระบบตรวจคำตอบและแสดงเฉลย (Answer Review) อย่างชัดเจน
- **หน้าแดชบอร์ด (Dashboard):** แสดงข้อมูลสรุปของผู้ใช้และเป็นจุดเริ่มต้นเข้าสู่แบบทดสอบ

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

- **React 19:** ไลบรารีหลักสำหรับการสร้าง User Interface แบบ Component-based
- **Vite:** เครื่องมือ Build tool ที่ช่วยให้การรันเซิร์ฟเวอร์ตอนพัฒนาและการสร้างโปรเจกต์รวดเร็วมากยิ่งขึ้น
- **React Router v7:** สำหรับจัดการการเปลี่ยนหน้า (Navigation) ในแอปพลิเคชันรูปแบบ Single Page Application (SPA)
- **Axios:** จัดการเรื่องการยิง HTTP Request ไปยัง Backend API ได้อย่างมีประสิทธิภาพ
- **Lucide React:** ไลบรารีสำหรับชุดไอคอนที่สวยงามและปรับแต่งได้ง่าย

## � วิธีการติดตั้งและรันโปรเจกต์ (Getting Started)

1. ตรวจสอบให้แน่ใจว่าติดตั้ง Node.js แล้ว
2. ติดตั้งแพ็กเกจ (Dependencies) ทั้งหมด:
   ```bash
   npm install
   ```
3. รัน Development Server:
   ```bash
   npm run dev
   ```
4. เปิดเบราว์เซอร์และเข้าไปที่ `http://localhost:5173`

## ☁️ การนำไปใช้งานจริงผ่าน Git (Deploying with Git)

โปรเจกต์นี้ถูกออกแบบมาเพื่อรองรับการ Deploy ผ่าน Git ไปยังโฮสติ้งยอดนิยมอย่าง **Vercel, Netlify, หรือ GitHub Pages** ได้อย่างง่ายดาย

**ขั้นตอนเบื้องต้นสำหรับการ Deploy:**
1. พุช (Push) โค้ดทั้งหมดของคุณขึ้นไปยัง GitHub, GitLab, หรือ Bitbucket
2. เชื่อมต่อ Repository ของคุณเข้ากับแพลตฟอร์มอย่าง Vercel หรือ Netlify
3. แพลตฟอร์มเหล่านั้นจะทำการ Build โค้ดโดยอัตโนมัติ โดยอ้างอิงจากคำสั่ง:
   - **Build Command:** `npm run build`
   - **Output Directory (โฟลเดอร์ผลลัพธ์):** `dist`

เพียงเท่านี้ เว็บไซต์ Frontend ของคุณก็พร้อมออนไลน์บนเซิร์ฟเวอร์จริงทุกครั้งที่คุณ Push โค้ดขึ้น Git!

> **หมายเหตุ:** สำหรับรายละเอียดการรันระบบโดยรวมพร้อมกับ Backend สามารถดูได้ที่ **[README หลักของโปรเจกต์](../README.md)** ในโฟลเดอร์ root
